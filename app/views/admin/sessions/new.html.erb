<%= form_tag admin_sessions_path, class:'form-signin' do %>
  <h2 class="form-signin-heading">TICS</h2>
  <% if flash[:notice] %>
    <div class="alert alert-success">
      <%= flash[:notice] %>
    </div>
  <% end %>
  <% if flash[:alert] %>
    <div class="alert alert-danger">
      <%= flash[:alert] %>
    </div>
  <% end %>
  <%= email_field_tag :email, params[:email], class:'form-control',
  placeholder:'Correo electrónico' %>
  <%= password_field_tag :password, '',class:'form-control', placeholder:'Contraseña' %>
</p>
  <%= submit_tag 'Ingresar', class:'btn btn-lg btn-primary btn-block' %>
<% end %>
